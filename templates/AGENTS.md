# AGENTS.md (template)

> **Stub.** The agent-neutral runbook handed to a nonprofit at the end of a build. Read by OpenAI Codex, Google Jules, GitHub Copilot, and Cursor. Keep in sync with `CLAUDE.md`.

The finished version covers: what the assistant does; how each scheduled job is wired; where to change the system prompt; how schema migrations are applied and rolled back; the safe-vs-escalate list; and the hard rule that the agent works on a branch and opens a pull request — it never pushes to the live system directly.
