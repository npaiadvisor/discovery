# CLAUDE.md (template)

> **Stub.** The Claude-specific runbook. Imports the content of `AGENTS.md` and adds any Claude Code-specific notes. Ship both files and keep them in sync so the choice of coding agent does not change the instructions.
